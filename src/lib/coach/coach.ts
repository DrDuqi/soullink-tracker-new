// Public entry for the Coach layer. Types live in ./types, the vendor-neutral providers in
// ./providers, the engine in ./engine, and the reactive React hook in hooks/useCoachReport.
// (The former deterministic buildCoachReport() now lives in providers/ruleBased.ts.)
export * from './types'
